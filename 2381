#include <bits/stdc++.h>
using namespace std;

int main() {
	 int n;
	 cin>>n;
	 int pos;
	 cin>>pos;
	 vector<string>myvet;
	 string nome;
	 for(int i=0;i<n;i++){
		cin>>nome;
		myvet.push_back(nome);
	 }
	 sort(myvet.begin(),myvet.end());
	 cout<<myvet[pos-1]<<'\n';
	return 0;
}
